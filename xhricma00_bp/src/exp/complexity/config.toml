# exp/complexity/config.toml
# Author: Marek Hric
# CPU benchmark

[complexity]
models = ["decision_tree", "gmm", "svm", "tcn", "tcn_s", "tcn_l"]

warmup_pushes = 10
trials = 5
pushes_per_trial = 200

seed = 0
input_scale = 0.3

torch_threads = 1

cleanup_gc_passes = 3
cleanup_sleep_s = 0.5
